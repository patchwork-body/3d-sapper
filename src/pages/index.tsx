import { CubeMatrix } from 'components/cube-matrix';

const RootPage = () => {
  return (
    <div
      style={{
        height: '100vh',
        width: '100vw',
      }}
    >
      <CubeMatrix />
    </div>
  );
};

export default RootPage;
